program Instalar;

uses
  Forms,
  AInstalar in 'AInstalar.pas' {FInicializa};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TFInicializa, FInicializa);
  FInicializa.Timer1.Interval := 1000;
  Application.Run;
end.
